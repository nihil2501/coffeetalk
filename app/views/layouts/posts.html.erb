<% content_for :sidebar do %>
  <%= render 'shared/sidebar/reading_list' %>

  <% current_user.organizations.each do |organization| %>
    <% content_for :group_li_links do %>
      <% organization.groups.each do |group| %>
        <%= render partial: 'shared/sidebar/group', locals: { group: group, active: group == @group } %>
      <% end %>
    <% end %>

    <%= render partial: 'shared/sidebar/organization', locals: { organization: organization, active: organization == @group.organization } %>
  <% end %>

  <%= render 'shared/sidebar/new_organization' %>
<% end %>

<%= render template: 'layouts/application' %>