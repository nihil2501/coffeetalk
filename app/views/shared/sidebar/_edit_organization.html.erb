var afterSidebar = [];

afterSidebar.push(function () {
  $collapse = $('#<%= organization.id %>-collapse');

  $collapse.find('ul.nav.nav-list').hide();
  $collapse.find('.manage-organization').show();
});